# ─── Supabase (REQUIRED) ──────────────────────────────────────────────────────
# Find these in: Supabase dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ─── Google AdSense (OPTIONAL — hosted version only) ─────────────────────────
# Leave VITE_ENABLE_ADS=false (or unset) to disable ads entirely.
# Self-hosted users never need to set these.
#
# Steps to get your IDs:
#  1. Sign up at https://adsense.google.com
#  2. Add and verify your custom domain
#  3. VITE_ADSENSE_CLIENT_ID → Publisher ID shown as ca-pub-XXXXXXXXXXXXXXXX
#  4. VITE_ADSENSE_SLOT_ID   → create an Ad Unit → copy the numeric slot ID
VITE_ENABLE_ADS=false
VITE_ADSENSE_CLIENT_ID=ca-pub-XXXXXXXXXXXXXXXX
VITE_ADSENSE_SLOT_ID=0000000000
